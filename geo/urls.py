from django.urls import path

from .views import (
    ASCrearCursoView,
    ASTodasView,
    AyudaView,
    CalendarioUpdate,
    CursoDeleteView,
    CursoDetailView,
    CursosTodosView,
    CursosPendientesView,
    ForanoDetailView,
    ForanoResolverView,
    ForanoSolicitarView,
    ForanoTodosView,
    HomePageView,
    MatricularPlanView,
    MisAsignaturasView,
    MisCursosView,
    ProfesorCursoAnularView,
    ProfesorCursoAnyadirView,
    ResolverSolicitudCursoView,
    SolicitarCursoNoRegladoView,
)


urlpatterns = [
    path('', HomePageView.as_view(), name='home'),
    path('ayuda/', AyudaView.as_view(), name='ayuda'),
    path('asignatura/<int:pk>/crear-curso/', ASCrearCursoView.as_view(), name='as-crear-curso'),
    path('asignatura/', ASTodasView.as_view(), name='as-todas'),
    path('gestion/calendario/<slug:slug>/', CalendarioUpdate.as_view(), name='calendario'),
    path('gestion/curso/<int:pk>/delete/', CursoDeleteView.as_view(), name='curso-delete'),
    path('gestion/curso/resolver/', ResolverSolicitudCursoView.as_view(), name='resolver-solicitud'),
    path('gestion/curso/<int:anyo_academico>/', CursosTodosView.as_view(), name='cursos-todos'),
    path('gestion/curso/', CursosTodosView.as_view(), name='cursos-todos'),
    path('gestion/curso/pendientes/', CursosPendientesView.as_view(), name='cursos-pendientes'),
    path('gestion/forano/<int:pk>/', ForanoDetailView.as_view(), name='forano-detail'),
    path('gestion/forano/', ForanoTodosView.as_view(), name='forano-todos'),
    path('gestion/forano/resolver/', ForanoResolverView.as_view(), name='forano-resolver'),
    path('gestion/matricular-plan/', MatricularPlanView.as_view(), name='matricular-plan'),
    path('gestion/profesor-curso/', ProfesorCursoAnyadirView.as_view(), name='pc-anyadir'),
    path('gestion/profesor-curso/<int:pk>/anular', ProfesorCursoAnularView.as_view(), name='pc-anular'),
    path('pod/', MisAsignaturasView.as_view(), name='mis-asignaturas'),
    path('curso/<int:pk>/', CursoDetailView.as_view(), name='curso-detail'),
    path('curso/', MisCursosView.as_view(), name='mis-cursos'),
    path('curso/solicitar/', SolicitarCursoNoRegladoView.as_view(), name='curso-solicitar'),
    path('usuario-externo/solicitar/', ForanoSolicitarView.as_view(), name='forano-solicitar'),
]
